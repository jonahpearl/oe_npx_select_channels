from setuptools import find_packages, setup

setup(
    name='oe_npx_selector',
    packages=find_packages(),
    version='0.0.1',
    description='Connect to Open Ephys and select channels on a Neuropixels probe',
    author='Jonah Pearl',
    license='MIT',
)