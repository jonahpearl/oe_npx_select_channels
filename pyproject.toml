[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "oe_npx_selector"
version = "0.1.0"
dependencies = [
    "requests>=2.28.0",
    "matplotlib",
    "numpy",  # hopefully dont need to pin below 2 but we shall see
    "open-ephys-python-tools",
]